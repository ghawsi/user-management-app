

Finalize the create process.
Finalize the Read process.
Finalize the Update process.
Finalize the disable process.

Redesign the user tables with all needed fields and other tables for ANSI RBAC. 
Create the models in draw.io


6. start working on backend
6.1. logout button on user part of the header
6.2. Read the users on users page and pass it to the child component as prop


5. deploy it in docker


Implement a global log mechanism to handle all WH questions about the user. Or to track user activity.


Context API is good for 
1. Logged-in user
2. Theme (Dark/Light mode)
3. Language
4. Currency
5. Permissions
6. Feature Flags
7. Application Configuration
// I think, right now it is no good option to handle forms through Context API.